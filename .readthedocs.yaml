# Read the Docs configuration for notfallmedizin
# https://docs.readthedocs.io/en/stable/config-file/v2.html

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_checkout:
      - pip install -e ".[dev]"
  sphinx:
    configuration: docs/conf.py

formats:
  - pdf
  - html
